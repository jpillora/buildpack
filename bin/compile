#!/bin/sh

if [ ! -f $1/bin/run ]; then
	echo "Missing $1/bin/run"
	exit 1
fi

echo "Run script '$1/bin/run'"
chmod +x $1/bin/run

if [ -f $1/bin/compile ]; then
	echo "compiling..."
	exec "$1"/bin/compile "$@"
	echo "compiled"
fi

exit 0

